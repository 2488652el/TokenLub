# Design

MoonMeter 的架构与设计规范：

- `ARCHITECTURE.md`：Electron 进程边界、数据层和安全模型。
- `PROVIDERS.md`：Provider 接入和价格目录规范。
- `MOTION.md`：动效语义、时长、性能边界和 reduced-motion 降级。
- `assets/`：应用图标、Logo 与打包视觉资源。
- `screenshots/`：公开文档使用的产品截图。

新增视觉规范、交互原则或设计规划时应先放在此目录，再由实现任务引用。
