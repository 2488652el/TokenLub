# Tests

- `unit/` — Vitest unit tests (parsers, cost engine, IPC schemas)
- `e2e/` — Playwright end-to-end against packaged Electron
- `fixtures/` — Sample API responses + JSONL files for parser tests

Phase A only scaffolds these directories.