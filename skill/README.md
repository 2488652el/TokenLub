# Skill

此目录只存放 TokenLub 项目需要调用的项目级 Skill。

每个 Skill 使用独立目录，入口文件必须命名为 `SKILL.md`：

```text
skill/
  release-check/
    SKILL.md
    scripts/
    references/
```

当前项目没有可迁移的 `SKILL.md`；后续新增时不要把全局个人 Skill、缓存或工具安装目录复制到这里。
