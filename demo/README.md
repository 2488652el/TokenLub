# Demo

临时验证、自动化测试和生成物集中区：

- `tests/`：Vitest 单元/集成测试和 Playwright E2E。
- `scripts/`：只用于演示、测试数据或临时验证的脚本。
- `out/`：Electron/Vite 编译输出（忽略提交）。
- `tokenlub-<版本号>-<修改说明>-<执行模型>/`：安装包目录（忽略提交）。
- `cache/`、`coverage/`、`test-results/`、`playwright-report/`：临时结果（忽略提交）。

这里的生成物不会进入 GitHub 发布区。
